spring:
  datasource:
    continue-on-error: true
    driver-class-name: org.postgresql.Driver
    initialization-mode: always
    url: url
    username: username
    password: password

bot:
  token: token
  username: username

target.chat.id: 0